import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 16,
    paddingTop: 72,
    backgroundColor: '#D6D6D6'
  },
  input: {
    marginBottom: 10
  }
});
